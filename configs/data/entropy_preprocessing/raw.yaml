# @package _global_

data:
  entropy_preprocessing:
      bin_entropies: False
      num_bins: 0
      
  train_dataset:
    bin_entropies: ${data.entropy_preprocessing.bin_entropies}
    num_bins: ${data.entropy_preprocessing.num_bins}

  val_dataset:
    bin_entropies: ${data.entropy_preprocessing.bin_entropies}
    num_bins: ${data.entropy_preprocessing.num_bins}