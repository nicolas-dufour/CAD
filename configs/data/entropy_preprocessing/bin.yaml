# @package _global_

data:
  entropy_preprocessing:
      bin_entropies: True
      num_bins: 10
      uniform_bins: False
      
  train_dataset:
    bin_entropies: ${data.entropy_preprocessing.bin_entropies}
    num_bins: ${data.entropy_preprocessing.num_bins}
    uniform_bins: ${data.entropy_preprocessing.uniform_bins}
    

  val_dataset:
    bin_entropies: ${data.entropy_preprocessing.bin_entropies}
    num_bins: ${data.entropy_preprocessing.num_bins}
    uniform_bins: ${data.entropy_preprocessing.uniform_bins}